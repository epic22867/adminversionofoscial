[phases.setup]
nixPkgs = ["nodejs_20", "python3", "python3Packages.pip"]

[phases.install]
cmds = [
  "npm install",
  "pip3 install duckduckgo-search --break-system-packages"
]

[start]
cmd = "node server.js"
